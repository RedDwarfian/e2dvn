let Aya = Character({
    id: 'aya',
    name: 'Aya',
    actor: 'Aya'
  },
  center,
  mood('Neutral'),
  invisible
);

show(bg, visible);
show(Aya, visible);
show(tb, visible);

absoluteCenter;
stageLeft;
stageRight;
inactive;
active;

Aya`Hello`;
