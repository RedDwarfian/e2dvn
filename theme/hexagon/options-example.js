
//section: Meta
config.about = `Created by jtenner. (c) jtenner 2016`;
config.themeVersion = '0.0.1';

//section: Text
config.fontSize = 16;
config.nameSize = 32;
config.interfaceTextSize = 36;
config.labelTextSize = 16;
config.notificationTextSize = 16;
config.titleTextSize = 64;

//to load a font use this line relative to the ./font/ folder
//require('./TimesNewRoman.ttf');

//use the font name here
config.font = 'monospace';
config.interfaceFont = 'monospace';
config.notificationFont = 'monospace';

//use rgbaArray, hsl(), rgba(), rgb(), hex('value'), or 'name'
config.textColor = [200, 200, 200, 0.7]; //rgba
config.textHoverColor = [255, 255, 255, 1];
config.textSelectedColor = [255, 255, 255, 1];

config.choiceColor = [200, 200, 200, 0.7];
config.choiceHoverColor = [255, 255, 255, 1];
config.choiceSelectedColor = [255, 255, 255, 1];

config.mutedColor = [155, 155, 155, 1];
config.mutedColorHover = [200, 200, 200, 1];


//section: Menus
//top left bottom right
config.menuBorder = [40, 40, 40, 40];
config.confirmFrameBorder = [60, 60, 60, 60];
config.tileFrameBackground = true;

//section controls: